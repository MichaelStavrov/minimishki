/**
 * Доменное имя сохранено для существующих сервисов услуг.
 * Фактическая очистка находится в common и используется всеми HTML-сущностями.
 */
export { sanitizeEditorHtml as sanitizeServiceHtml } from '../../common/editor-html.sanitizer';
