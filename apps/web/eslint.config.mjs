// Локальная точка входа ESLint для apps/web.
// Все правила живут в общем пакете: packages/eslint-config/next.js
// (он же подключает base.js, правила Next.js, FSD и eslint-config-prettier).
import next from '@minimishki/eslint-config/next';

export default next;
