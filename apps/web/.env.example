# ---------------------------------------------------------------------------
# Шаблон переменных окружения для apps/web (Next.js).
# Реальные значения — в apps/web/.env.local (в git не попадает).
#   cp apps/web/.env.example apps/web/.env.local
# ---------------------------------------------------------------------------

# Базовый адрес API. Порт 3001 — из apps/api/.env (PORT),
# путь /api — глобальный префикс Nest, он задаётся на шаге 15.
# Префикс NEXT_PUBLIC_ означает, что значение попадёт в браузерный бандл.
NEXT_PUBLIC_API_URL=http://localhost:3001/api
